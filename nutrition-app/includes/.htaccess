# Empêche l'accès direct aux fichiers PHP internes (doivent être appelés via require)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
